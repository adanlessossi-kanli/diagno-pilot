[pytest]
testpaths = tests
asyncio_mode = strict
asyncio_default_fixture_loop_scope = function
filterwarnings =
    ignore:MONGODB_URI host.*cannot be resolved:UserWarning
markers =
    integration: marks tests as requiring Docker (deselect with -m "not integration")
